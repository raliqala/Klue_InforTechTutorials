
@{
    ViewBag.Title = "Index";
}


@*Networks*@

<style>
    .ProgrammingLinks ul {
        display: inline-flex;
        list-style: none;
    }

        .ProgrammingLinks ul li a {
            text-decoration: none;
            background-color: #2246f2;
            color: white;
            padding: 1em 1.5em;
            text-decoration: none;
            text-transform: uppercase;
            border-radius: unset;
            margin-left: 1rem;
            width: 10em;
        }

    .ProgrammingLinks ul {
        width: 25rem;
    }

    .container .row-button a {
        margin: 20rem;
    }
</style>

<div style="margin-top:5rem;"></div>
<div class="container">
    <div class="row">
        <div class="jumbotron" style="border-radius: unset;">
            <div>Programming courses</div>
        </div>
    </div>
</div>

<div class="container">
    <h2>Networking courses</h2>
    <p>Click any button to access any course:</p>
    <div class="btn-group btn-group-sm">
        <a href="@Url.Action("Mathematics", "NetworkingCourses")" class="btn btn-primary">Mathematics</a>
        <a href="@Url.Action("SystemsDevelopment", "NetworkingCourses")" class="btn btn-primary">Systems Development</a>
        <a href="@Url.Action("InternetIntranetandExtranet", "NetworkingCourses")" class="btn btn-primary">Internet, Intranet, and Extranet </a>
        <a href="@Url.Action("InformationSystemsinOrganizations", "NetworkingCourses")" class="btn btn-primary">Information Systems in Organizations</a>
        <a href="@Url.Action("DataCommunicationNetworks", "NetworkingCourses")" class="btn btn-primary">Data Communication Networks</a>
        <a href="@Url.Action("CommunicationNetwork", "NetworkingCourses")" class="btn btn-primary">Communication Network</a>
        <a href="@Url.Action("NetworkAchitecture", "NetworkingCourses")" class="btn btn-primary">Network Achitecture</a>
    </div>
</div>

<div style="margin-bottom: 5rem;"></div>
